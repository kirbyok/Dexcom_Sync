services:
  dexcom-sync:
    build: .
    container_name: dexcom-sync
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
    command: ["python", "main.py", "continuous"]
